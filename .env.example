# Copy to .env.local for local dev. Values come from `supabase start` (F-02).
# The service role key is NEVER placed here and never gets a NEXT_PUBLIC_ prefix.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
